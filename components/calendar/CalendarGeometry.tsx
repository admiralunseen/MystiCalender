import type { CalendarEvent } from "@/types/event"; export type CalendarScale="day"|"week"|"month"|"year"; export function CalendarGeometry({view,events,onSelect}:{view:CalendarScale,events:CalendarEvent[],onSelect:(e:CalendarEvent)=>void}){if(view==="day")return <div className="geometry daySpiral"><div className="nowCore">NOW<span>EVER-PRESENT</span></div>{events.map((e,i)=>{const a=i/events.length*Math.PI*7-.8,r=10+i/events.length*38,x=50+r*Math.cos(a),y=50+r*Math.sin(a);return <button key={e.id} className="eventNode" style={{left:`${x}%`,top:`${y}%`}} onClick={()=>onSelect(e)}>✦<span>{e.title}</span></button>})}</div>;if(view==="week")return <div className="geometry weekWheel"><div className="nowCore">ONE FIELD<span>SEVEN DOORS</span></div>{events.slice(0,7).map((e,i)=>{const a=i/7*Math.PI*2-Math.PI/2,x=50+39*Math.cos(a),y=50+39*Math.sin(a);return <button key={e.id} className="weekNode" style={{left:`${x}%`,top:`${y}%`}} onClick={()=>onSelect(e)}>{["SUN","MON","TUE","WED","THU","FRI","SAT"][i]}<span>{e.title}</span></button>})}</div>;if(view==="month")return <div className="geometry monthWheel"><div className="nowCore">THE MONTH<span>ENTER · TEND · RELEASE</span></div>{events.map((e,i)=>{const a=i/events.length*Math.PI*2,x=50+42*Math.cos(a),y=50+42*Math.sin(a);return <button key={e.id} className="monthNode" style={{left:`${x}%`,top:`${y}%`}} onClick={()=>onSelect(e)}>✦</button>})}</div>;return <div className="geometry yearWheel"><div>Q1 · EMERGENCE</div><div>Q2 · EXPANSION</div><div>Q3 · EXPRESSION</div><div>Q4 · INTEGRATION</div><div className="nowCore">THE YEAR<span>THE SPIRAL CONTINUES</span></div></div>}
